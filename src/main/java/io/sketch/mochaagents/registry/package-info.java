/**
 * Composition helpers: catalog/registry style factories so agents, CLI and examples do not duplicate
 * switch-on-string construction logic (maintains compatibility with GOF Registry + Abstract Factory usages).
 */
package io.sketch.mochaagents.registry;
